let express = require('express');
// let controller = require('../controller');
let apiRoutes = express.Router();

// auth apis
// apiRoutes.post("/auth/login", controller.auth.POST);

module.exports = apiRoutes;